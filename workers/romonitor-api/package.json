{
  "name": "@worldmonitor/romonitor-api-worker",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "test": "node --test index.test.mjs",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
